export enum Providers {
  FS = 'fs',
  S3 = 's3',
  DB = 'db',
}

export enum SourceTypes {
  GPKG = 'geopackage',
  S3 = 's3',
  FS = 'file',
}

export enum TileFormats {
  JPEG = 'image/jpeg',
  PNG = 'image/png',
}
